# TruthLens-UA — необхідні дії та розрахунковий час

**Оновлено:** 08.03.2026  

---

## Необхідний час (що залишилось)

За поточним станом проєкту (дані є, модель натренована, GitHub і GitLab синхронізовані) **орієнтовний час** для решти кроків:

| Що залишилось | Необхідний час |
|---------------|----------------|
| ~~Ноутбук 03 (A/B, MLflow)~~ | ✅ Виконано |
| ~~Деплой Render~~ | ✅ Виконано (truthlens-ua.onrender.com Live) |
| Прогнати демо-тексти через API на Render | **5–10 хв** |
| Слайди та тезисний план розділу 3 | **1–2 год** |
| **Разом до повного готового захисту** | **~1,5–2 год** (без написання тексту диплому) |

---

## Виконано

| Дія | Стан |
|-----|------|
| Git init, коміти, remotes | ✅ |
| Push у GitHub | ✅ |
| Push у GitLab | ✅ |
| Завантаження ISOT (Fake.csv, True.csv) | ✅ |
| Ноутбук 01, artifacts/best_model.joblib | ✅ |
| Ноутбук 03 (A/B, MLflow) | ✅ |
| Деплой Render (truthlens-ua.onrender.com) | ✅ |

---

## Залишилось зробити (таблиця з часом)

| № | Дія | Розрахунковий час | Деталі |
|---|-----|-------------------|--------|
| ~~1~~ | ~~Ноутбук 03 (A/B, MLflow)~~ | ✅ Виконано | — |
| ~~1~~ | ~~Деплой Render~~ | ✅ Виконано | https://truthlens-ua.onrender.com Live |
| 1 | **Демо-тексти через API на Render** | **5–10 хв** | `python scripts/demo_api.py https://truthlens-ua.onrender.com` |
| 2 | **Слайди та план розділу 3** | **1–2 год** | За мастер-промптом; тезисний план для захисту. |

---

## Сумарний необхідний час (підсумок)

| Сценарій | Час |
|----------|-----|
| **Технічно до MVP** (деплой + демо) | ✅ **Готово** (Render Live) |
| **Прогнати демо на Render** | **~5–10 хв** |
| **Повна підготовка до захисту** (слайди, план розділу 3) | **~1,5–2 год** |

---

## Швидкі команди

```bash
# Перевірка стану:
python scripts/next_step.py

# Оновлення запису стану (після змін):
python scripts/update_project_state.py

# Синхронізація репо:
git add -A && git commit -m "опис" && git push origin main && git push gitlab main
```

Детальні кроки: **docs/DEPLOYMENT.md**, **docs/CONTINUE_AFTER_PAUSE.md**.
